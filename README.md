# Task_1: Generic Box Container

Простой и мощный обобщенный контейнер для хранения объектов любого типа в Java.

## Описание

Класс `Box<T>` представляет собой универсальный контейнер, который может безопасно хранить один объект любого типа. Реализация обеспечивает типобезопасность, проверки состояний.

## Функциональность

### Основные методы класса `Box<T>`

- **`put(T item)`** - поместить объект в коробку
- **`get()`** - извлечь объект (коробка очищается)
- **`peek()`** - посмотреть объект без извлечения
- **`isEmpty()`** - проверить, пуста ли коробка
- **`isFull()`** - проверить, заполнена ли коробка
- **`toString()`** - получить строковое представление

# Task_2: Generic Storage<T> - Умное хранилище с null-безопасностью

Универсальный обобщенный контейнер для безопасной работы с потенциально null-значениями через систему альтернативных значений.

## 🚀 О проекте

Класс `Storage<T>` решает одну из самых распространенных проблем в Java - **NullPointerException** (NPE). Он предоставляет элегантный механизм для работы с значениями, которые могут быть `null`.

## 📋 Основная концепция

### Что такое NullPointerException?
**NPE** - это исключение, которое возникает при попытке использовать объект, равный `null`. Например:
```java
String text = null;
System.out.println(text.length()); // ❌ NullPointerException!
```
### Основные методы класса `Box<T>`

- **`get()`** - возвращает значение ()

- **`getOriginal()`** - возвращает исходное значение (даже если оно null)

- **`isNull()`** - проверяет контент на null

# Task_3 Generic Box System with Wildcards

Расширенная система обобщенных контейнеров с демонстрацией wildcards (масок типов) в Java Generics.

## 🚀 Обзор проекта

Этот проект демонстрирует  **нижних ограниченных масок (lower bounded wildcards)** `<? super T>` в Java Generics через практические примеры с классами `BoxNew<T>` и `Point3D`.

## 📚 Основные концепции

### Wildcards (Маски типов)
- **`<? super T>`** - нижняя ограниченная маска (может принимать T и его супертипы)
- **Безопасность типов** на уровне компиляции

### Иерархия типов
- Object (супертип всех классов)
- ↑
- Number (супертип числовых классов)
- ↑
- Integer, Double, etc.
- ↑
- Point3D (НЕ наследует Number!)

### Используем класс Point, который хранит в себе точки координат, и Box<T> для хранения
### Используем для тестирования рандомные значения от 0 до 10

# Task_4

Этот проект демонстрирует лямбду функцию

