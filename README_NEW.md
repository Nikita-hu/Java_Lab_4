# Java Generics & Functional Interfaces - Полный проект

Комплексный проект для изучения **Java Generics** (обобщения), **null-безопасности**, **wildcards**, и **функциональных интерфейсов** с практическими примерами.

---

## 📁 Структура проекта

### Основные классы и интерфейсы

#### 1. **Box.java** - Обобщенный контейнер
Простой и мощный обобщенный контейнер для хранения объектов любого типа.

**Функциональность:**
- `put(T item)` - поместить объект в коробку
- `get()` - извлечь объект (коробка очищается)
- `peek()` - посмотреть объект без извлечения
- `isEmpty()` - проверить, пуста ли коробка
- `isFull()` - проверить, заполнена ли коробка
- `toString()` - получить строковое представление

**Особенности:**
- Типобезопасность
- Проверка состояний (не может положить в заполненную коробку)
- Поддержка любых типов через генерики `<T>`

---

#### 2. **Storage.java** - null-безопасное хранилище
Универсальный контейнер для работы с потенциально null-значениями через систему альтернативных значений.

**Функциональность:**
- `get()` - возвращает значение или альтернативу, если значение null
- `getOriginal()` - возвращает исходное значение (даже если оно null)
- `isNull()` - проверяет, содержится ли null

**Решаемая проблема:**
Предотвращает `NullPointerException` при работе с потенциально null-значениями.

```java
String text = null;
Storage<String> storage = new Storage<>(text, "default");
System.out.println(storage.get()); // Выведет "default" вместо NPE
```

---

#### 3. **BoxNew.java** - Расширенный контейнер
Практически идентичен `Box<T>`, используется для демонстрации **wildcards** (`<? super T>`).

**Ключевая концепция:**
Демонстрирует работу с нижними ограниченными масками типов.

---

#### 4. **Point3D.java** - 3D точка в пространстве
Класс для представления точек в трехмерном пространстве.

**Свойства:**
- `x`, `y`, `z` - координаты точки

**Методы:**
- Конструкторы (пустой и с параметрами)
- Геттеры и сеттеры для координат
- `distanceToOrigin()` - вычисление расстояния до начала координат
- `toString()` - строковое представление точки

**Используется для:**
- Демонстрации работы с пользовательскими типами в generic контейнерах
- Тестирования с рандомными значениями от 0 до 10

---

#### 5. **Function.java** - Функциональный интерфейс
Функция, преобразующая значение типа T в значение типа R.

```java
@FunctionalInterface
public interface Function<T, R> {
    R apply(T value);
}
```

**Использование:**
Применение трансформации к значениям в коллекциях и потоках данных.

---

#### 6. **Predicate.java** - Проверяющий функциональный интерфейс
Интерфейс для проверки условий (предикатов).

```java
@FunctionalInterface
public interface Predicate<T> {
    boolean test(T value);
}
```

**Использование:**
Фильтрация элементов по условиям.

---

#### 7. **Reducer.java** - Функция свертки
Интерфейс для сворачивания (reduce) коллекции значений.

```java
@FunctionalInterface
public interface Reducer<T> {
    T reduce(T accumulator, T current);
}
```

**Использование:**
Агрегирование значений (сумма, произведение, объединение и т.д.).

---

### Main.java - Главная программа

Интерактивное меню для выбора задач:

- **1.1** - Демонстрация работы класса `Box<T>`
- **1.2** - Дополнительные примеры с `Box<T>`
- **2.3** - Демонстрация класса `Storage<T>`
- **3.1** - Работа с `BoxNew<T>` и `Point3D`
- **3.2** - Дополнительные примеры с wildcards
- Демонстрация функциональных интерфейсов

---

## 🎯 Основные концепции

### Java Generics (Обобщения)
- Обеспечивают **типобезопасность** на уровне компиляции
- Позволяют создавать универсальные контейнеры
- Синтаксис: `ClassName<TypeParameter>`

### Wildcards (Маски типов)
- **`<? super T>`** - нижняя ограниченная маска (может принимать T и его супертипы)
- **`<? extends T>`** - верхняя ограниченная маска (может принимать T и его подтипы)
- **`<?>`** - неограниченная маска (любой тип)

### Функциональные интерфейсы
- Интерфейсы с одним абстрактным методом
- Используются с **лямбда-выражениями** и **method references**
- Поддерживаются декоратором `@FunctionalInterface`

---

## 🚀 Как запустить

```bash
javac *.java
java Main
```

Затем выберите нужную задачу из предложенного меню.

---

## 📚 Учебные цели

✅ Понимание и применение Java Generics  
✅ Работа с null-значениями безопасно  
✅ Использование wildcards в generics  
✅ Функциональное программирование в Java  
✅ Применение функциональных интерфейсов  

---

**Разработано для практического изучения продвинутых возможностей Java.**
